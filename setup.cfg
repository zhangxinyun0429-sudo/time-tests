[coverage:report]
# 排除不需要覆盖的代码行
exclude_lines =
    pragma: no cover
    if __name__ == .__main__.:
    raise NotImplementedError
    raise AssertionError

[coverage:run]
# 不统计测试文件本身
omit =
    test_*.py
